import { Inter, Cinzel } from 'next/font/google'

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
})

export const cinzel = Cinzel({
    subsets: ['latin'],
    display: 'swap',
})
